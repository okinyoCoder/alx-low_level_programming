#include "main.h"

/**
 * _abs -  computes the absolute value of an integer
 *
 * @n: checks the absolute value
 *
 * Return: returns absolute value
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
