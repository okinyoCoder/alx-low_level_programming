#include "main.h"

/**
 * main - test whether random number is positive or negative
 *
 * Return: always 0 when success
 */
int main(void)
{
	int i;

	i = 98;
	positive_or_negative(i);

	return (0);
}
