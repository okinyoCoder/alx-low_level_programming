#include<stdio.h>

/**
 * main -  prints all single digit numbers of base 10 starting from 0
 * Return: returns zero
 */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}

	return (0);
}
